spring:
  application:
    name: amigoSecreto
  datasource:
    url: jdbc:postgresql://localhost:3333/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true

